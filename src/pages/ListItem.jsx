import React from "react";

export default function ListItem() {
  return (
    <div>
      <h1>List an Item</h1>
      <p style={{ color: "#444" }}>
        Placeholder. Next step: create the listing form and add it to mock data (then later DB).
      </p>
    </div>
  );
}
